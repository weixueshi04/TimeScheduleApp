# 🚀 第一周快速开始指南

## 欢迎！

恭喜你决定开发这个APP！这份指南将帮助你顺利完成第一周的任务。第一周的目标是：**搭建项目基础架构**。

---

## 📋 第一周任务清单

### Day 1-2: 环境搭建与项目创建

#### ✅ 任务1.1：安装Flutter开发环境（2小时）

**Windows系统：**
```bash
# 1. 下载Flutter SDK
# 访问：https://flutter.dev/docs/get-started/install/windows
# 下载 flutter_windows_xxx.zip

# 2. 解压到合适目录（如 C:\src\flutter）

# 3. 配置环境变量
# 将 C:\src\flutter\bin 添加到PATH

# 4. 验证安装
flutter doctor
```

**macOS系统：**
```bash
# 使用Homebrew安装（推荐）
brew install flutter

# 或者手动下载
# 访问：https://flutter.dev/docs/get-started/install/macos

# 验证安装
flutter doctor
```

**重要提示：**
- `flutter doctor`会检查缺失的依赖，按提示安装
- 至少安装一个平台的开发工具（Android或iOS）
- 建议同时安装Android Studio和VS Code

---

#### ✅ 任务1.2：创建项目（30分钟）

```bash
# 1. 创建Flutter项目
flutter create focus_life

# 2. 进入项目目录
cd focus_life

# 3. 运行默认项目，测试环境
flutter run
```

**验证成功标志：**
- 能看到默认的计数器APP
- 点击按钮，数字能够增加

---

### Day 3-4: 配置依赖与目录结构

#### ✅ 任务1.3：配置项目依赖（1小时）

编辑 `pubspec.yaml` 文件：

```yaml
name: focus_life
description: 时间与健康管理APP
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.6
  
  # 状态管理
  provider: ^6.1.1
  
  # 本地存储
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  shared_preferences: ^2.2.2
  
  # UI组件
  table_calendar: ^3.0.9
  fl_chart: ^0.66.0
  
  # 通知
  flutter_local_notifications: ^16.3.0
  
  # 音频
  audioplayers: ^5.2.1
  
  # 工具类
  intl: ^0.18.1
  uuid: ^4.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  
  # Hive代码生成
  hive_generator: ^2.0.1
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
  
  # 资源文件
  assets:
    - assets/images/
    - assets/sounds/
  
  # 字体配置（可选）
  # fonts:
  #   - family: SF Pro
  #     fonts:
  #       - asset: fonts/SF-Pro-Display-Regular.otf
```

**安装依赖：**
```bash
flutter pub get
```

---

#### ✅ 任务1.4：创建项目目录结构（1小时）

在 `lib/` 目录下创建以下结构：

```bash
# 在项目根目录执行
mkdir -p lib/core/constants
mkdir -p lib/core/themes
mkdir -p lib/core/utils
mkdir -p lib/core/services
mkdir -p lib/data/models
mkdir -p lib/data/repositories
mkdir -p lib/data/local
mkdir -p lib/business/providers
mkdir -p lib/business/services
mkdir -p lib/presentation/screens/home
mkdir -p lib/presentation/screens/tasks
mkdir -p lib/presentation/screens/focus
mkdir -p lib/presentation/screens/health
mkdir -p lib/presentation/screens/statistics
mkdir -p lib/presentation/widgets
mkdir -p lib/presentation/navigation
mkdir -p assets/images
mkdir -p assets/sounds
```

**目录说明：**
- `core/`: 核心层，存放常量、主题、工具类
- `data/`: 数据层，存放数据模型和存储逻辑
- `business/`: 业务逻辑层，状态管理
- `presentation/`: 展示层，UI界面和组件

---

### Day 5-6: 设计主题与导航框架

#### ✅ 任务2.1：创建主题配置（2小时）

创建 `lib/core/themes/app_theme.dart`：

```dart
import 'package:flutter/cupertino.dart';

/// APP主题配置（iOS风格）
class AppTheme {
  // 主色调
  static const Color primaryColor = CupertinoColors.systemBlue;
  static const Color secondaryColor = CupertinoColors.systemGreen;
  
  // 功能色
  static const Color successColor = CupertinoColors.systemGreen;
  static const Color warningColor = CupertinoColors.systemOrange;
  static const Color dangerColor = CupertinoColors.systemRed;
  
  // 背景色
  static const Color backgroundColor = CupertinoColors.systemBackground;
  static const Color cardColor = CupertinoColors.secondarySystemBackground;
  
  // 文字色
  static const Color primaryTextColor = CupertinoColors.label;
  static const Color secondaryTextColor = CupertinoColors.secondaryLabel;
  
  // 字体大小
  static const double fontSizeTitle = 28.0;
  static const double fontSizeSubtitle = 20.0;
  static const double fontSizeBody = 16.0;
  static const double fontSizeCaption = 14.0;
  static const double fontSizeSmall = 12.0;
  
  // 间距
  static const double spacingXS = 4.0;
  static const double spacingS = 8.0;
  static const double spacingM = 12.0;
  static const double spacingL = 16.0;
  static const double spacingXL = 24.0;
  static const double spacingXXL = 32.0;
  
  // 圆角
  static const double radiusS = 8.0;
  static const double radiusM = 12.0;
  static const double radiusL = 16.0;
  
  // 按钮高度
  static const double buttonHeight = 44.0;
  
  /// iOS主题数据
  static CupertinoThemeData get lightTheme {
    return const CupertinoThemeData(
      primaryColor: primaryColor,
      scaffoldBackgroundColor: backgroundColor,
      barBackgroundColor: cardColor,
      textTheme: CupertinoTextThemeData(
        primaryColor: primaryTextColor,
        textStyle: TextStyle(
          fontSize: fontSizeBody,
          color: primaryTextColor,
        ),
      ),
    );
  }
}
```

---

#### ✅ 任务2.2：创建底部导航栏（2小时）

创建 `lib/presentation/navigation/main_tab_navigator.dart`：

```dart
import 'package:flutter/cupertino.dart';
import 'package:focus_life/core/themes/app_theme.dart';
import 'package:focus_life/presentation/screens/home/home_screen.dart';
import 'package:focus_life/presentation/screens/tasks/task_list_screen.dart';
import 'package:focus_life/presentation/screens/focus/focus_screen.dart';
import 'package:focus_life/presentation/screens/health/health_screen.dart';
import 'package:focus_life/presentation/screens/statistics/statistics_screen.dart';

/// 主导航TabBar
class MainTabNavigator extends StatefulWidget {
  const MainTabNavigator({super.key});

  @override
  State<MainTabNavigator> createState() => _MainTabNavigatorState();
}

class _MainTabNavigatorState extends State<MainTabNavigator> {
  int _currentIndex = 0;

  // 所有页面
  final List<Widget> _pages = [
    const HomeScreen(),
    const TaskListScreen(),
    const FocusScreen(),
    const HealthScreen(),
    const StatisticsScreen(),
  ];

  @override
  Widget build(BuildContext context) {
    return CupertinoTabScaffold(
      tabBar: CupertinoTabBar(
        currentIndex: _currentIndex,
        onTap: (index) {
          setState(() {
            _currentIndex = index;
          });
        },
        backgroundColor: AppTheme.cardColor,
        activeColor: AppTheme.primaryColor,
        inactiveColor: AppTheme.secondaryTextColor,
        items: const [
          BottomNavigationBarItem(
            icon: Icon(CupertinoIcons.home),
            label: '首页',
          ),
          BottomNavigationBarItem(
            icon: Icon(CupertinoIcons.list_bullet),
            label: '任务',
          ),
          BottomNavigationBarItem(
            icon: Icon(CupertinoIcons.timer),
            label: '专注',
          ),
          BottomNavigationBarItem(
            icon: Icon(CupertinoIcons.heart_fill),
            label: '健康',
          ),
          BottomNavigationBarItem(
            icon: Icon(CupertinoIcons.chart_bar),
            label: '统计',
          ),
        ],
      ),
      tabBuilder: (context, index) {
        return CupertinoTabView(
          builder: (context) => _pages[index],
        );
      },
    );
  }
}
```

---

#### ✅ 任务2.3：创建占位页面（1小时）

创建五个占位页面，先显示简单文字：

**lib/presentation/screens/home/home_screen.dart**
```dart
import 'package:flutter/cupertino.dart';
import 'package:focus_life/core/themes/app_theme.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return CupertinoPageScaffold(
      navigationBar: const CupertinoNavigationBar(
        middle: Text('FocusLife'),
      ),
      child: SafeArea(
        child: Center(
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              Icon(
                CupertinoIcons.home,
                size: 80,
                color: AppTheme.primaryColor,
              ),
              const SizedBox(height: 20),
              const Text(
                '欢迎使用FocusLife',
                style: TextStyle(
                  fontSize: AppTheme.fontSizeTitle,
                  fontWeight: FontWeight.bold,
                ),
              ),
              const SizedBox(height: 8),
              Text(
                '开始你的高效生活',
                style: TextStyle(
                  fontSize: AppTheme.fontSizeBody,
                  color: AppTheme.secondaryTextColor,
                ),
              ),
            ],
          ),
        ),
      ),
    );
  }
}
```

**类似地创建其他四个页面：**
- `lib/presentation/screens/tasks/task_list_screen.dart`（任务页）
- `lib/presentation/screens/focus/focus_screen.dart`（专注页）
- `lib/presentation/screens/health/health_screen.dart`（健康页）
- `lib/presentation/screens/statistics/statistics_screen.dart`（统计页）

---

#### ✅ 任务2.4：修改主入口（30分钟）

修改 `lib/main.dart`：

```dart
import 'package:flutter/cupertino.dart';
import 'package:focus_life/core/themes/app_theme.dart';
import 'package:focus_life/presentation/navigation/main_tab_navigator.dart';

void main() {
  runApp(const FocusLifeApp());
}

class FocusLifeApp extends StatelessWidget {
  const FocusLifeApp({super.key});

  @override
  Widget build(BuildContext context) {
    return CupertinoApp(
      title: 'FocusLife',
      theme: AppTheme.lightTheme,
      home: const MainTabNavigator(),
      debugShowCheckedModeBanner: false,
    );
  }
}
```

---

### Day 7: 测试与总结

#### ✅ 任务3.1：运行测试（1小时）

```bash
# 运行APP
flutter run

# 热重载快捷键
# 在终端按 r 键
# 或在IDE中按 Ctrl+S (Windows) / Cmd+S (Mac)
```

**测试要点：**
- [x] APP能正常启动
- [x] 底部导航栏显示正常
- [x] 可以切换不同Tab
- [x] 每个页面显示对应的占位内容
- [x] iOS风格UI正确显示

---

#### ✅ 任务3.2：代码优化（1小时）

1. **格式化代码**
```bash
# 格式化所有Dart文件
flutter format lib/
```

2. **运行代码检查**
```bash
# 检查代码问题
flutter analyze
```

3. **修复警告和错误**

---

#### ✅ 任务3.3：Git版本管理（30分钟）

```bash
# 初始化Git仓库
git init

# 添加所有文件
git add .

# 首次提交
git commit -m "feat: 初始化项目，完成基础架构搭建

- 配置项目依赖
- 创建目录结构
- 实现底部导航栏
- 创建5个占位页面
- 配置iOS风格主题"

# （可选）关联远程仓库
# git remote add origin <你的仓库地址>
# git push -u origin main
```

---

## 🎉 第一周完成检查清单

完成以下所有项，你就成功完成了第一周的任务！

- [ ] Flutter开发环境配置完成，`flutter doctor`无错误
- [ ] 项目成功创建并能运行
- [ ] 所有依赖包安装成功
- [ ] 项目目录结构创建完整
- [ ] 主题配置文件创建完成
- [ ] 底部导航栏实现并正常工作
- [ ] 5个占位页面创建完成
- [ ] APP能在模拟器/真机上正常运行
- [ ] 代码格式化且无警告
- [ ] Git提交完成

---

## 💡 常见问题与解决方案

### Q1: flutter doctor显示Android相关错误
**A:** 
```bash
# 安装Android Studio
# 在Android Studio中：
# Tools -> SDK Manager -> 安装 Android SDK

# 配置环境变量（Windows）
ANDROID_HOME=C:\Users\你的用户名\AppData\Local\Android\sdk

# 接受许可协议
flutter doctor --android-licenses
```

---

### Q2: iOS相关错误（仅macOS）
**A:**
```bash
# 安装Xcode（从App Store）
# 安装命令行工具
sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer
sudo xcodebuild -runFirstLaunch

# 安装CocoaPods
sudo gem install cocoapods
```

---

### Q3: 依赖包安装失败
**A:**
```bash
# 清理缓存
flutter clean

# 重新获取依赖
flutter pub get

# 如果还不行，删除pubspec.lock后重试
rm pubspec.lock
flutter pub get
```

---

### Q4: 模拟器无法启动
**A:**
```bash
# 查看可用设备
flutter devices

# 创建新的Android模拟器
flutter emulators

# iOS模拟器（macOS）
open -a Simulator
```

---

### Q5: 热重载不生效
**A:**
- 确保使用的是 `flutter run` 而不是 `flutter build`
- 某些修改（如main函数、依赖包）需要完全重启（按R键）
- 检查是否有语法错误

---

## 📚 本周学习要点总结

### 你学到了什么？

1. **Flutter项目结构**
   - lib/：所有Dart代码
   - pubspec.yaml：依赖配置
   - assets/：资源文件

2. **Cupertino组件**
   - CupertinoApp：APP根组件
   - CupertinoPageScaffold：页面脚手架
   - CupertinoNavigationBar：导航栏
   - CupertinoTabScaffold：Tab导航
   - CupertinoTabBar：底部导航栏

3. **Flutter开发流程**
   - 创建项目
   - 配置依赖
   - 编写代码
   - 热重载调试
   - 版本管理

---

## 🚀 下周预告

下周（第2周），我们将开始开发**任务管理模块**：

1. 设计任务数据模型
2. 集成Hive数据库
3. 实现任务列表UI
4. 实现任务添加功能
5. 实现任务删除与编辑

**准备工作：**
- 复习Dart面向对象编程
- 了解Provider状态管理基础
- 学习Hive数据库基本用法

---

## 🎯 激励与建议

### 给自己的话

🌟 **恭喜你完成第一周！** 这是最重要的一步，很多人都倒在了环境配置上。

💪 **遇到问题很正常！** 每个开发者都会遇到bug，关键是如何解决。记住：
- 先看错误提示
- Google/百度搜索错误信息
- 查看官方文档
- 在社区提问

⏰ **保持节奏！** 建议：
- 每天至少2小时编码
- 遇到困难不要放弃
- 完成比完美更重要

🎊 **奖励自己！** 完成第一周后，可以：
- 吃顿好的
- 看场电影
- 和朋友分享你的成果

---

## 📞 需要帮助？

遇到问题可以：
1. 查看Flutter官方文档
2. 在GitHub上搜索相关issue
3. 在Stack Overflow提问
4. 加入Flutter中文社区

**记住：没有解决不了的问题，只有不够努力的搜索！😄**

---

## ✅ 本周任务完成标志

当你能做到以下几点，说明第一周任务完美完成：

1. ✅ 在手机/模拟器上看到你的APP运行
2. ✅ 可以点击底部导航栏切换页面
3. ✅ 每个页面显示不同的内容
4. ✅ 代码组织清晰，无明显错误
5. ✅ 理解了Flutter的基本概念

---

**准备好了吗？开始第二周的冒险吧！🚀**

记住：**代码改变世界，坚持成就梦想！**
